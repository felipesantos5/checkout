version: "3.8"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    env_file: .env
    environment:
      - TZ=America/Sao_Paulo
    restart: unless-stopped
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
