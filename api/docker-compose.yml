version: "3.8"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4242:4242"
    env_file: .env
    environment:
      - TZ=America/Sao_Paulo
    restart: unless-stopped
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
